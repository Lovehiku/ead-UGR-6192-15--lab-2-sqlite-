
---

### **2️⃣ SQLite EF Core Minimal API (PizzaStoreSqlite)**

```markdown
# PizzaStore SQLite API

This is a Minimal API using **EF Core with SQLite** for storing pizzas persistently.

## Features
- List all pizzas (`GET /pizzas`)
- Get a single pizza (`GET /pizza/{id}`)
- Add a new pizza (`POST /pizza`)
- Update a pizza (`PUT /pizza/{id}`)
- Delete a pizza (`DELETE /pizza/{id}`)
- Swagger UI available at `/swagger`
- Persistent database using SQLite (`Pizzas.db`)

## Setup
1. Clone the repo
2. Navigate to the project folder
   ```bash
   cd PizzaStoreSqlite
